# === Rust ===
# Generated by Cargo; compiled files and executables
debug
target

# Backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these
*.pdb

# Generated by cargo mutants
**/mutants.out*/

# === Tooling ===
# mise local overrides (machine-specific)
.mise.local.toml
mise.local.toml

# Built documentation site (mdBook). Only the book source (docs/book/book.toml + docs/book/src/)
# is committed; the generated HTML is regenerated by `mise run docs` / CI.
docs/book/book/

# Dropped in the CWD by `openapi-generator-cli` when `scripts/compare-generators.sh` invokes it
# (see docs/benchmarks.md). A machine-local launcher config, not a project artifact.
openapitools.json

# === macOS ===
.DS_Store
__MACOSX/
.AppleDouble
.LSOverride
Icon[]
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# === Linux ===
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*
nohup.out
