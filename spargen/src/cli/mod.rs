//! # Subsystem: cli (feature `cli`)
//! layer-deps: facade
//!
//! The command surface (`generate` / `check` / `explain`), the exit-code contract, and
//! `--format json` (PRD §2.3, FR6). Depends only on the crate facade.
