//! # Subsystem: emit
//! layer-deps: codegen, diag
//!
//! Output layout (module vs standalone crate), `Cargo.toml` synthesis, the provenance header,
//! and `--check` drift diffing (PRD §2.3, §2.1).
